extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 Hey there, welcome to my Fullstack Developer page!

      p.lead.text-light
        | This is a showcase page for some of my best projects from 2018.
        | This site uses express to render pug templates, and create different routes in the browser, sending the user to different sites.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- Use the Pug `each` iterator to create a project img, title and link in the markup for each of the projects you are showcasing.

      //- Use a combination of interpolation and template literals as needed to dynamically generate the the routes in the href, as well as the img's src and the projects title.

      //- Here's what the Pug for each project example should look like.
      each val, index in data.projects
        .cell
          a(href=`/project/${val.id}`)
            img.thumbnail(src=`${val.image_url[0]}`)
            h5 #{val.project_name}
